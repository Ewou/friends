<!DOCTYPE html>
<html>
  <head>
    <title>My Friends</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="container">
  <nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
      <p class="navbar-brand"><%= app_title %></p>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <%= render 'shared/nav_link', label: 'All', path: root_path %>
          <%= render 'shared/nav_link', label: 'Party', path: party_friends_path %>
          <%= render 'shared/nav_link', label: 'Work', path: work_friends_path %>
          <%= render 'shared/nav_link', label: 'School', path: school_friends_path %>
          <%= render 'shared/nav_link', label: 'Holidays', path: holidays_friends_path %>
          <%= render 'shared/nav_link', label: 'Other', path: other_friends_path %>
        </div>
      </div>
    </div>
  </nav>
    <%= yield %>
  </body>
</html>
